#include <stdio.h>

int main()
{
  int a,b,q,t;
  
  printf("Enter the two numbers:");
  scanf("%d%d",&a,&b);
  t=a;
  while((a-b)>=0)
  {
    q++;
    a-=b;
  }
  printf("division of %d and %d is%d\n",t,b,q);
  printf("Modulo %d\n",a);
  return 0;
}
